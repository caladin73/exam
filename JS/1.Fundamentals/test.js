waldo = 'white';
fred = 'black';
// is a sequence

waldo = 'black';
fred = 'white';
// is another sequence


var a = 1;
var b = 2;
if (a {
    var text = “I know a < b, it’s a bit obvious”;
    document.getElementById(“demo”).innerHTML =text;
}

if (d.getTime() > 6 && d.getTime() < 18) {
    waldo = 'white';
    fred = 'black';
} else {
    waldo = 'black';
    fred = 'white';
}

qux = Math.rand();
if (qux < 0.5) {
    heads = heads + 1;
} else {
    tails = tails + 1;
}

count = 0;
while (count < 10) {
    qux = Math.random();
    if (qux < 0.5) {
        heads = heads + 1;
    } else {
        tails = tails + 1;
    } count = count + 1;
}


if (qux < 0.5) { // condition
    heads = heads + 1; // code if true
} else {
    tails = tails + 1; // code if false
}